---
trigger: always_on
---

All features must match web standards behavior.
If behavior differs from specs, it is considered a bug.

No silent failures.
Every failure must log explicitly.

No speculative implementations.
If spec is unclear:

Document ambiguity

Implement spec-compliant behavior only
