---
trigger: always_on
---

Features must be implemented at 100%.

No partial implementations
No “basic version”
No placeholders

A feature is complete only when all sub-features are complete.
Completion means full spec coverage.

Only after full completion may a feature be struck off.

Strike through the feature in FENBROWSER_COMPLETE_ANALYSIS.md

Do not delete the entry

Update feature completion percentage immediately.
Example:

HTML Rendering → 100%

Update total lines of code after feature completion.
