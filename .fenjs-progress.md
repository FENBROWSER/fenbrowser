# FenJS Revamp Progress

**Plan:** `C:\Users\udayk\Downloads\FenBrowser_CSharp_JS_Engine_Revamp_Test262_First.md`
**Branch:** `new` (revamp branch, 145k+ insertions vs `main`)
**Last updated:** 2026-05-23
**Last commit on branch:** `8bb30d8a` — fenjs: remove variable store from runtime frames

> Resume protocol: read this file first, find the `NEXT` marker, confirm with the user before executing.
> Don't restart from a `[x]` step. Don't silently re-do completed work.

---

## A. Milestone status

| ID  | Milestone | Status | Notes |
|-----|-----------|--------|-------|
| 0.1 | Shell + test262 foundation | [x] DONE | Projects, source manager, diagnostics, logging, test262 runner skeleton all present. |
| 0.2 | Lexer/parser + parser test262 | [~] HARDENING | Lexer + parser functional; last 5 commits = unicode identifier / template substitution / escape hardening. Driving parser-only test262 conformance up. |
| 0.3 | Bytecode + basic interpreter | [x] DONE | OpCode, Instruction, BytecodeFunction, BytecodeVerifier, BytecodeCompiler, BytecodeInterpreter all wired. Missing opcodes: `Await`, `Yield`, `CreateClass`, `EnterScope`/`LeaveScope` — defer until 0.5/0.6 features land. |
| 0.4 | Logical heap + GC + object semantics | [~] PARTIAL | Heap/RootSet/HandleScope/HeapVerifier/GC-stress ✅. **Missing: WriteBarrier API stub (§8.3), explicit mark-sweep collector pass.** Object descriptors + prototypes ✅. |
| 0.5 | Functions, closures, classes | [~] PARTIAL | Closures ✅, accessor get/set ✅, arguments object ✅, Function.prototype ✅. **Missing: class syntax→runtime (commit `c0b53340`-ish referenced ParserOnly classes), `super`, bound functions.** |
| 0.6 | Promises + modules | [ ] **NOT STARTED** | `HeapCellKind.Promise` / `HeapCellKind.Module` enum stubs exist but no `PromiseObject`, `PromiseCapability`, `PromiseReaction`, `JobQueue`, `ModuleRecord`, `ImportEntry`, `ExportEntry`, `ModuleEnvironment`. |
| 0.7 | FenBrowser host integration | [ ] NOT STARTED | `HostObjectHandle` value type exists. **Missing: `HostObjectTable`, `IHostHooks`, navigation epoch enforcement, document/frame lifetime checks in renderer integration.** |
| 0.8 | test262 serious mode | [~] PARTIAL | Dashboard writer, expectations, gate verifier ✅. Daily run + regression gate + crash triage = wire up CI workflow. |
| 0.9 | Inline caches | [ ] NOT STARTED | Premature — wait until 0.6/0.7 done per plan §31. |
| 1.0 | Production interpreter | [ ] NOT STARTED | Aggregate of 0.6–0.9. |

---

## B. Plan-section audit (sections 5–33)

Format: `§N Title — STATUS — files / gaps`

- §5 `JsValue` — [x] DONE — `Runtime/JsValue.cs`, `Runtime/JsValueTag.cs` (all 10 tags incl. BigInt).
- §5.2 `ObjectHandle` — [x] DONE — `Runtime/ObjectHandle.cs`.
- §5.3 `HostObjectHandle` — [x] DONE — `Runtime/HostObjectHandle.cs`.
- §6 Logical JS Heap — [x] DONE — `Heap/JsHeap.cs`, `HeapCell.cs`, `HeapCellKind.cs`, `ITraceable.cs`, `IHeapTracer.cs`.
- §7 Handle Scopes — [x] DONE — `Heap/Handle.cs`, `HandleScope.cs`, `RootSet.cs`, `AllocationSite.cs`.
- §8 GC Strategy — [x] DONE — `Heap/HeapVerifier.cs`, `GcStressMode.cs`, mark-sweep collector at `JsHeap.CollectGarbage()`, `WriteBarrier(owner, child)` API at `JsHeap.cs:159` (called from 22 property/prototype-store sites in `BytecodeInterpreter.cs`, verified by `HeapVerifier`, covered by tests in `RuntimeHeapTests` + `ObjectAndBytecodeTests`).
- §9 Reentrancy `[MayExecuteJs]` — [x] DONE — `Runtime/MayExecuteJsAttribute.cs`.
- §10 Lexer — [~] HARDENING — `Lexer/JsLexer.cs`, `Token.cs`, `TokenKind.cs`. Active work area; many recent fixes. Source spans + structured diagnostics in place.
- §11 Parser — [x] DONE (functional) — `Parser/JsParser.cs`, `JsParserException.cs`, `UnsupportedFeatureException.cs`. Pratt expressions + statements + functions + arrows + with-statements all parse.
- §12 AST Validation + Early Errors — [x] DONE — `AstValidation/AstValidator.cs`, `FeatureSupportLevel.cs`, `UnsupportedFeatureDiagnostic.cs`.
- §13 Bytecode IR — [~] PARTIAL — `Bytecode/OpCode.cs` (52 opcodes), `Instruction.cs`, `BytecodeFunction.cs`, `BytecodeVerifier.cs`, `BytecodeCompiler.cs`. **GAP vs plan §13.1: `Await`, `Yield`, `CreateClass`, `EnterScope`/`LeaveScope` opcodes missing. Defer until 0.5/0.6 features land.**
- §14 Interpreter — [x] DONE — `Interpreter/BytecodeInterpreter.cs`, `InterpreterFrame.cs`, `JsThrownException.cs`, `VariableStore.cs`. Verify: instruction budget / interrupt callback / termination flag (plan §14.2) — spot check needed before claiming 0.4 complete.
- §15 Object Model — [x] DONE — `Objects/JsObject.cs`, `JsPropertyDescriptor.cs`, `JsFunctionObject.cs`. Confirm all 11 internal methods of §15.3 covered (`[[Get]]`/`[[Set]]`/`[[DefineOwnProperty]]`/etc.).
- §16 Arrays — [~] PARTIAL — Array element storage path lives inside `JsObject`. **Verify: `length` is special, holes ≠ undefined, non-configurable element blocks length truncation. No dedicated `ArrayElements.cs`.**
- §17 Strings — [~] PARTIAL — `Runtime/StringHandle.cs` + heap-allocated strings ✅. **No string interner / no separate source-text storage.**
- §18 Functions — [~] PARTIAL — `Objects/JsFunctionObject.cs` ✅. Confirm `FunctionKind` covers ordinary/arrow/method/constructor/bound/native. **`async`/`generator` kinds depend on 0.6.**
- §19 Environments + Scope — [ ] **MISSING** — Plan §19 mandates `GlobalEnvironment`/`DeclarativeEnvironment`/`ObjectEnvironment`/`FunctionEnvironment`/`ModuleEnvironment` types. Current code uses flat `Interpreter/VariableStore.cs` + `Runtime/JsVariableCell.cs`. **This is the highest-priority architectural debt** — Modules + classes + TDZ all depend on it.
- §20 Builtins — [ ] **MISSING FRAMEWORK** — Recent commits added Math/JSON/RegExp/SyntaxError/Function.prototype but inlined in `Interpreter/BytecodeInterpreter.cs`, NOT in a `Builtins/` framework. Plan §20.2 mandates per-builtin spec citation, test262 file list, side-effect docs. **Needs extraction into `Builtins/` with proper framework.**
- §21 Promises + Microtasks — [ ] **MISSING** — `HeapCellKind.Promise` enum value exists, no payload type. No `PromiseObject`, `PromiseCapability`, `PromiseReaction`, `PromiseJob`, `JobQueue`, `HostPromiseRejectionTracker`. **Largest 0.6 gap.**
- §22 Modules — [ ] **MISSING** — `HeapCellKind.Module` enum value exists, no payload. No `ModuleRecord`, `SourceTextModuleRecord`, `ImportEntry`, `ExportEntry`, `ModuleEnvironment`, `ModuleMap`, `HostResolveImportedModule`.
- §23 Host Bindings — [ ] **MISSING** — `HostObjectHandle` value type exists. **No `IHostHooks`, no `HostObjectTable`, no document/frame epoch validation logic.**
- §24 Web IDL Binding Contract — [~] PARTIAL — `FenBrowser.WebIdl*` (old engine) exists. New `docs/webidl/webidl_binding_contract.md` per plan = check.
- §25 test262 Strategy — [x] DONE — `Test262Runner`, `Test262Frontmatter`, `Test262Expectations`, `Test262DashboardWriter`, `Test262GateVerifier`, `Test262Manifest`, `Test262ResultWriter` all present. Strong subsystem.
- §26 Differential Testing — [ ] NOT STARTED — No `fenjs-compare` tool, no engines.json. Low priority until 0.6.
- §27 Fuzzing — [ ] **MISSING PROJECT** — Plan §3 lists `FenBrowser.Js.Fuzz` (LexerFuzz, ParserFuzz, BytecodeFuzz, RuntimeFuzz, HostBindingFuzz). Project not created. Recommend after 0.6.
- §28 Logging — [x] DONE — `Logging/JsLog.cs`, `JsLogCategories.cs`. Source-generated.
- §29 Security architecture — [~] PARTIAL — Threat model + unsafe policy docs ✅. Host isolation table = blocked on §23.
- §30 Performance — n/a — Tracked separately in [js_engine_perf_baseline].
- §31 Inline caches — [ ] DELAYED per plan.
- §32 JIT — [ ] DELAYED per plan.
- §33 WebAssembly — [ ] DELAYED per plan.

---

## C. Recommended next steps (ordered queue)

The architectural critical-path is: **Environments → Builtins framework → Promises → Modules → Host integration → IC**. Lexer hardening can continue in parallel as small fix commits.

- [x] **Step A — `WriteBarrier` API stub (plan §8.3)** — already in place before this session. Verified 2026-05-22. (Audit-correction, no new commit.)
- [~] **Step B: Environment record types (plan §19)** — types done, interpreter wiring pending. Subcommits:
  - [x] B.1 `EnvironmentRecord` + `DeclarativeEnvironmentRecord` + `BindingOpResult` — commit `77b9c92f` (2026-05-22). 17 tests.
  - [x] B.2 `ObjectEnvironmentRecord` + `IBindingObject` — commit `f6a82a9c`. 14 tests.
  - [x] B.3 `FunctionEnvironmentRecord` + `ThisBindingStatus` — commit `6b7cb2f9`. 7 tests.
  - [x] B.4 `GlobalEnvironmentRecord` (routing + this + VarNames; `CreateGlobalVarBinding`/`CanDeclareGlobalVar`/`HasRestrictedGlobalProperty` deferred to B.4.5) — commit `d2d74eb6`. 10 tests.
  - [x] B.5 `ModuleEnvironmentRecord` + `CreateImportBinding` + base `GetThisBinding` virtual — commit `fd500faa`. 8 tests.
  - [~] **Step B.6**: wire the records into `BytecodeCompiler` + `BytecodeInterpreter`. **High risk** — touches the hot path.
    - [x] B.6.1 `JsObjectBindingAdapter` over `JsObject` — commit `cff24418`. 11 tests.
    - [x] B.6.2 `EnvironmentRecord` field on `InterpreterFrame` — commit `908ee8e6`. No behavior change yet.
    - [x] B.6.3 `LoadName`/`StoreName` shim in `BytecodeInterpreter` routing `LoadVar`/`StoreVar` through `frame.Environment` with VariableStore fallback; new `SlotNameTable` reverse-map. Commit `8a1f52e4`. 6 new tests (919 main + 35 fuzz = 954 total). Non-mirroring writes — env path activates only when later commits insert bindings explicitly; closure capture stays on shared cells until B.6.4.
    - [x] B.6.4 Route closure capture through the lexical env-record chain. `JsFunctionObject.OuterEnvironment` captured at CreateFunction; callee frame's env built with that as OuterEnv. Parameters + `arguments` are now live env-record bindings. `LoadName`/`StoreName` walk the OuterEnv chain per ECMA-262 9.1.2.1. Local `var`/`let` slots still flow through VariableStore until B.6.6. Commit `156cbf2c`. 6 new tests (1533 main + 35 fuzz = 1568 total).
    - [x] B.6.5 Migrate global-object property reads/writes to `GlobalEnvironmentRecord` routing. Top-level `Execute` now uses the realm global env record, standard globals are installed as global object properties, and `globalThis.name` / bare global lookup share the same binding surface. Commit `898a2743`. 5 new tests, exact `built-ins/global/global-object.js` + `property-descriptor.js` test262 rechecks.
    - [x] B.6.6 Delete `VariableStore` and `JsVariableCell`; runtime frame identifier operations now route through environment records. Sub-slices: `BytecodeFunction.VarDeclarationNames`, env-record var instantiation, global `eval` path, `$262.evalScript` prelude (commit `55d168c4`); lexical/const declaration metadata, `InitVar`, TDZ/const-assignment binding errors (commit `f1841652`); function declaration hoisting through environment-backed bindings (commit `6f508f5c`); captured-cell closure map removal + env-record GC tracing (commit `2d5a197c`); redundant builtin slot preloads removed in favor of global-object environment resolution (commit `782846a7`); final `VariableStore` deletion with unresolvable-reference/delete handling (commit `8bb30d8a`).
  - [x] **B.4.5** Global declaration-instantiation ops (`HasRestrictedGlobalProperty`, `CanDeclareGlobalVar`, `CreateGlobalVarBinding`) + `IGlobalObject` interface + `JsObject.Extensible` — commit `de128c3f`. 6 new tests. `CanDeclareGlobalFunction` and `CreateGlobalFunctionBinding` still deferred to B.4.6 (needed only by `globalThis.foo = function`-style script init).
- [ ] **Step C: Builtins framework (plan §20)**. ~1-2 days. Create `Builtins/` folder + `IBuiltinModule` interface with ECMA-262 §-reference attribute. Extract Math / JSON / SyntaxError / Function.prototype / accessor helpers out of `BytecodeInterpreter.cs`. Per-builtin test class.
- [x] **Step D: Promises + microtasks (plan §21)** — types complete; interpreter wiring still pending (depends on B.6).
  - [x] D.1 `PromiseObject` + `PromiseState` — commit `c66920be`. 7 tests.
  - [x] D.2 `PromiseCapability` — commit `65a1aa19`. 4 tests.
  - [x] D.3 `PromiseReaction` + `PromiseReactionType` — commit `29ab29be`. 4 tests.
  - [x] D.4 `JobQueue` + `PromiseJob`/`PromiseReactionJob`/`PromiseResolveThenableJob` — commit `bfc167ec`. 6 tests.
  - [x] D.5 `IHostPromiseRejectionTracker` + `InMemoryPromiseRejectionTracker` + `PromiseRejectionOperation` — commit `9198cfea`. 4 tests.
  - [x] D.6 done — Promise constructor + Promise.resolve/reject + Promise.prototype.then/catch/finally wired through real PromiseObject + JobQueue; `PerformPromiseThen`, `CreateResolvingFunctions`, `FulfillPromise`/`RejectPromise`, and `PromiseResolveThenableJob` deferral all spec-shaped; microtask checkpoint at end of `Execute` drains queueMicrotask host queue + PromiseJob queue interleaved until both empty; unhandled-rejection tracker fired on settle and on late handler attach. Commit `e717a110`. 14 new `PromiseRuntimeTests`. 1750 main + 35 fuzz green. **Remaining open async items** (none block D.6): `Promise.all` / `Promise.allSettled` / `Promise.race` / `Promise.any` statics; `async`/`await` desugar — both depend on Step H (function-kind expansion) and Step E.6 respectively.
- [x] **Step E: Module records (plan §22)** — types complete; full link-evaluate algorithm waits on B.6 + a `JsParser.ParseModule` → ImportEntry/ExportEntry extractor.
  - [x] E.1 `ImportEntry` + `ExportEntry` — commit `fea804cd`. 8 tests.
  - [x] E.2 `ModuleRecord` abstract + `ModuleStatus` + `ResolvedBinding` — commit `1a38e8c5`. 6 tests.
  - [x] E.3 `SourceTextModuleRecord` (full `GetExportedNames` + `ResolveExport` with cycle detection) — commit `85df5de2`. 9 tests.
  - [x] E.4 `ModuleMap` (per-realm cache, identity-stable) — commit `7c4b9397`. 5 tests.
  - [x] E.5 `IHostModuleResolver` + `InMemoryModuleResolver` — commit `40abeaac`. 3 tests.
  - [ ] E.6 (deferred): real Link/Evaluate that drives `ModuleEnvironmentRecord.CreateImportBinding` against resolved targets; needs interpreter.
  - [ ] E.7 (deferred): parser hook that emits ImportEntry/ExportEntry lists during `JsParser.ParseModule`.
- [x] **Step F: Host bindings (plan §23)** — table + hooks landed; interpreter integration deferred.
  - [x] F.1 `HostObjectKind` + `HostObjectEntry` — commit `cc380deb`. 3 tests.
  - [x] F.2 `HostObjectTable` + `HostObjectResolveContext` + `HostObjectResolution` — commit `505fd48e`. 7 tests. Generation/realm/document-epoch/navigation-epoch all validated.
  - [x] F.3 `IHostHooks` + `StandaloneHostHooks` — commit `220192c7`. 5 tests.
  - [x] F.4 `NavigationEpoch` + `DocumentEpoch` value types — commit `24d14431`. 6 tests.
  - [x] **F.5** done — `GetReceiverProperty` HostObject branch + `SetPropByName` opcode HostObject branch funnel through `HostObjectTable.Resolve` against a per-interpreter `HostResolveContext`, then defer to `IHostHooks.TryGet/SetHostProperty`. Invalid resolutions (stale generation, cross-realm, navigated-away document/frame) surface as JS TypeError before the embedder pointer is touched. `RegisterGlobalHostObject(name, handle)` seam wires host handles into the global. Commit `fc0b8ef5`. 9 new `HostObjectIntegrationTests`. 1759 main + 35 fuzz green.
- [x] **Step G: Fuzz project (plan §27)** — `FenBrowser.Js.Fuzz` test project online with 4 harnesses.
  - [x] G.1 Project scaffold + `LexerFuzz` (ascii/unicode/structured corpora, token-count bound) — commit `00f487f5`. 8 tests.
  - [x] G.2 `ParserFuzz` (ascii/structured/js-like corpora, deep-paren regression) — commit `6698c8b3`. 8 tests.
  - [x] G.3 `BytecodeFuzz` (random `BytecodeFunction` → verifier rejection survival) — commit `82ed3938`. 6 tests.
  - [x] G.4 `RuntimeFuzz` (parse+compile+verify on noise; full execute on a curated bounded corpus) — commit `87e112d5`. 13 tests. Notes the plan §14.2 instruction-budget gap that blocks fuzzing valid-but-infinite code.
- [~] **Step H: Class syntax → runtime (plan §0.5 finish)**.
  - [x] **H.1** parser + compiler for constructor + instance methods + static methods. AST `ClassMemberNode` + `ClassMemberKind`. Compiler lowers each class into existing opcodes (CreateFunction + NewObject + SetPropByName) — no new opcodes needed. Class declarations bound via `InitVar`/lexical name. Class expressions integrate with the normal expression pipeline. Commit `5c368f52`. 10 new `ClassRuntimeTests`. 1769 main + 35 fuzz green.
  - [ ] **H.2** `extends` prototype-chain inheritance — needs a `SetPrototype` opcode (or NativeFunctionObject hook) to wire `class.prototype.[[Prototype]] = base.prototype` and `class.[[Prototype]] = base` at runtime.
  - [ ] **H.3** `super(...)` constructor call + `super.method()` dispatch — needs home-object tracking on the active frame and a new AST node for super-expressions.
  - [ ] **H.4** getter/setter semantic dispatch (parser already produces them but compiler installs them as plain methods).
  - [ ] **H.5** computed keys, private fields, static initialization blocks (long tail).
- [ ] **Step I: Differential testing tool (plan §26)**. Low priority. `tools/fenjs-compare`.

**Background, in parallel:** continue lexer/parser hardening for parser-only test262 subset (the work the last 5 commits were doing). Pause whenever Steps A-F need momentum.

---

## D. Log of completed sessions

- 2026-05-22 — Initial audit + progress ledger. Audit-correction: §8 GC/WriteBarrier is fully done (`JsHeap.cs:159`, 22 call sites in interpreter, tests in `RuntimeHeapTests`/`ObjectAndBytecodeTests`). Confirmed real gaps: Environments (§19), Builtins framework (§20), Promises (§21), Modules (§22), Host bindings (§23) — no classes exist.
- 2026-05-22 — B.1 done. `EnvironmentRecord` abstract + `DeclarativeEnvironmentRecord` + `BindingOpResult`. Commit `77b9c92f`. 17 new tests, full 780-test suite green. Pushed.
- 2026-05-22 — B.2 done. `ObjectEnvironmentRecord` + `IBindingObject` interface. Commit `f6a82a9c`. 14 new tests (794 total). Pushed.
- 2026-05-22 — B.3 done. `FunctionEnvironmentRecord` + `ThisBindingStatus`. Un-sealed `DeclarativeEnvironmentRecord` to allow inheritance per spec. Commit `6b7cb2f9`. 7 new tests (801 total). Pushed.
- 2026-05-22 — B.4 done. `GlobalEnvironmentRecord` (routing/this/VarNames slice; declaration-instantiation ops deferred to B.4.5). Commit `d2d74eb6`. 10 new tests (811 total). Pushed.
- 2026-05-22 — B.5 done. `ModuleEnvironmentRecord` with `CreateImportBinding` + indirect resolution. Promoted `GetThisBinding` to base virtual; overrides added to Function/Global/Module. Commit `fd500faa`. 8 new tests (819 total). Pushed. **Session checkpoint — all five ECMA-262 §9.1.1 env record types now exist as standalone types; nothing wired into the interpreter yet. Next session = B.6 migration, planned in 6 sub-commits.**
- 2026-05-22 — B.6.1 done. `JsObjectBindingAdapter` bridges heap-resident `JsObject` to the `IBindingObject` surface used by env records. Re-resolves the handle on every call so reentrancy can't see stale state. Commit `cff24418`. 11 new tests (830 total). Pushed.
- 2026-05-22 — B.6.3 done. `LoadName`/`StoreName` shim in `BytecodeInterpreter` now funnels every LoadVar/StoreVar through `frame.Environment.GetBindingValue`/`SetMutableBinding` with the VariableStore fallback for unmigrated paths. First attempt mirrored every store into the env record and broke 5 closure/accessor tests (outer frames saw stale env copies because closure capture still flows through shared JsVariableCells); switched to a non-mirroring design that only activates the env path when a binding already exists. `SlotNameTable` ConditionalWeakTable cache builds the slot→name reverse map once per `BytecodeFunction`. Commit `8a1f52e4`. 6 new tests; 919 main suite + 35 fuzz = 954 total green. Pushed.
- 2026-05-22 — **Second aggressive batch session: 20 commits (2d347bfd → 3495e854).**
  - B.4.6 done — GlobalEnvironmentRecord.CanDeclareGlobalFunction + CreateGlobalFunctionBinding (commit `2d347bfd`)
  - Builtins framework C.1-C.4 — EcmaSpecReferenceAttribute, IBuiltinModule + BuiltinBinding, BuiltinRegistry, GlobalConstantsBuiltin (commits `ed4a…` → `ce9b62bd`). Framework is registry-driven and tested standalone; integration with the interpreter's existing inline EnsureXxxObject paths is deferred (single-source extraction lands when the inline factories are migrated wholesale).
  - Number constants — Number.EPSILON/MAX_SAFE_INTEGER/MIN_SAFE_INTEGER and Number.isFinite/isInteger/isNaN/isSafeInteger statics
  - Object statics — Object.is, Object.keys/values/entries, Object.getPrototypeOf/setPrototypeOf/create, Object.assign, Object.preventExtensions/isExtensible/freeze/isFrozen/seal/isSealed, Object.getOwnPropertyNames, Object.fromEntries
  - Array — Array.isArray; Array.prototype.join/indexOf/includes (with new SameValueZero static helper)
  - Math — sign/trunc/cbrt/log2/log10/hypot/clz32/imul/fround/sinh/cosh/tanh/asinh/acosh/atanh/expm1/log1p (with new ToUint32/ToInt32 helpers)
  - Global numeric functions — parseInt/parseFloat/isNaN/isFinite (full spec semantics including radix prefix handling, longest-valid-prefix parseFloat, Number.parseInt/parseFloat aliases point at the same ObjectHandle)
  - Bug fixed mid-session: lambda-parameter `_` shadowed the C# discard pattern; rewrote `_ = obj.DefineOwnProperty(...)` to bare expression statements where the result is intentionally ignored.
  - Tests: 919 → 1156 in main suite (+237 across 17 new test files, all xUnit). 35 fuzz tests unchanged. **1191 total green, all pushed.**
- 2026-05-22 — **Third aggressive batch session: 15 commits.** All Array.prototype + Function.prototype.{apply,bind} + String.from{CharCode,CodePoint} + Object.getOwnPropertyDescriptors. Array.prototype additions span the entire ES2023 surface: mutators (pop/shift/unshift/reverse/fill/sort/splice/copyWithin), non-mutating (slice/concat), iteration with callbacks (forEach/map/filter/reduce/reduceRight/every/some/find/findIndex/findLast/findLastIndex/flat/flatMap), search (lastIndexOf), at, plus statics (Array.of/Array.from with array-like form). All callback methods route through a new InvokeArrayCallback helper that surfaces non-function callbacks as TypeError. CreateArrayFromElements helper (always element-list semantics, never length-shortcut) added so Array.of and the new prototype methods don't trip the Array constructor's "single Number = length" rule. Tests: 1156 → 1287 (+131 across 15 new test files). 35 fuzz tests unchanged. **1322 total green, all pushed.**
- 2026-05-22 — **Fourth batch: 10 commits.** Object.defineProperties + Object.hasOwn (ES2022); Math.random + Date.now; Number.prototype.toFixed + toExponential + toPrecision (with NormaliseExponential helper for canonical e+N form); Number.prototype.toString with radix arg (LongToRadixString + 52-digit fractional emit for non-decimal); Error.prototype.toString + default name/message on Error.prototype; Date.UTC + Date.parse; globalThis global binding wired in InitializeBuiltinGlobals (read-back via globalThis.x works; bare-identifier read still needs B.6.5 routing - documented in the test file); Array.prototype.toLocaleString as toString fallback (no Intl); JSON.parse reviver argument with full bottom-up InternalizeJSONProperty walk including empty-key root wrapper. Tests: 1287 → 1357 (+70 across 10 new test files). **1392 total green, all pushed.**
- 2026-05-22 — **Aggressive batch session: 20 commits.** B.6.2 (frame env field), B.4.5 (global declaration ops + IGlobalObject + JsObject.Extensible), D.1–D.5 (entire Promise type family + JobQueue + rejection tracker), E.1–E.5 (entire Modules type family with full GetExportedNames/ResolveExport algorithm), F.1–F.4 (Host bindings: HostObjectTable with full lifetime validation, IHostHooks, epoch primitives), G.1–G.4 (FenBrowser.Js.Fuzz project + 4 harnesses: Lexer/Parser/Bytecode/Runtime). Commits `908ee8e6` → `87e112d5`. Test count 830 → 913 in main test suite, +35 in new Fuzz project = 948 total green. All pushed. Risky interpreter migrations (B.6.3-B.6.6, full D.6/E.6/F.5) deliberately deferred to dedicated sessions.
- 2026-05-23 — **ES2025 + Date mutation batch: 10 commits (`e14caa35` → `62da7469`).** Added: Set.prototype.union, intersection, difference, symmetricDifference, isSubsetOf, isSupersetOf, isDisjointFrom (ES2025 Set Methods proposal — all drain the other operand via CreateForOfIterator); DateObject [[DateValue]] becomes mutable + Date.prototype.setTime (TimeClip-applied, RequireDate helper); Date.prototype.setFullYear/setMonth/setDate plus setUTC* (shared SetDateField helper, NaN-resurrect on setFullYear per spec, ripple via AddMonths/AddDays); Date.prototype.setHours/setMinutes/setSeconds/setMilliseconds plus setUTC* (shared SetTimeField, multi-component trailing args, MakeTime overflow); structuredClone global (deep clone with cycle memoisation, Date/RegExp/Array/plain objects; functions/Symbols raise DataCloneError); RegExp.escape (ES2025; ASCII-letter/digit first char gets \\xHH form); queueMicrotask global (pending queue drained at end of every top-level Execute). 1647 → 1701 main tests (+54), 35 fuzz unchanged, **1736 total green**. All pushed.

- 2026-05-23 — **High-value ES surface batch: 20 commits (`182dfc8f` → `c6d59cb4`).** Added: encodeURI/encodeURIComponent/decodeURI/decodeURIComponent + URIError (full UTF-8 encoder/decoder, lone-surrogate detection); Date.prototype getTime/valueOf + toISOString + toJSON + getFullYear/getMonth/getDate/getDay/getHours/getMinutes/getSeconds/getMilliseconds + the UTC variants + getTimezoneOffset + toString/toDateString/toTimeString/toUTCString (engine LocalTZA=0 so local==UTC); Array.prototype toReversed/toSorted/toSpliced/with (ES2023, non-mutating); ReferenceError + EvalError + AggregateError native errors (AggregateError drains iterable .errors through CreateForOfIterator); WeakRef + FinalizationRegistry minimal (no GC integration; spec contract preserved); Symbol.for + Symbol.keyFor (paired Dictionary registry, O(1) round-trip); Object.groupBy + Map.groupBy (ES2024, route through CreateForOfIterator); JSON.stringify space-indent + replacer (function transformer + Array<String|Number> PropertyList) with full per-key recursive walk; String.prototype.normalize (NFC/NFD/NFKC/NFKD via .NET String.Normalize, RangeError on bad form); String.raw (template.raw array-like with substitution interleaving). 1533 → 1647 main tests (+114), 35 fuzz unchanged, **1682 total green**. All pushed.

- 2026-05-23 — **B.6.4 done.** `JsFunctionObject` gained `OuterEnvironment`; `CreateFunction` opcode snapshots `frame.Environment`; `ExecuteInternalCore` builds the callee frame's env as a fresh `DeclarativeEnvironmentRecord` chained to that outer env; parameters + `arguments` are now installed as live env-record bindings via `CreateMutableBinding` + `InitializeBinding`; `LoadName`/`StoreName` walk `OuterEnv` per ECMA-262 9.1.2.1 instead of only the local env. `CallFunction`/`ExecuteConstruct` thread `fn.OuterEnvironment` through. Closures now resolve outer-function parameters through the env chain rather than only through the legacy `JsVariableCell` snapshot; the snapshot remains as a parallel fallback until B.6.6. Commit `156cbf2c`. 6 new tests in `ClosureEnvChainTests` covering closure-read, nested-closure walk, closure-write, arrow capture, distinct-invocation independence, and peer-function resolution. 1527 → 1533 main + 35 fuzz = 1568 total green. Pushed pending. **Open risks for B.6.5:** outer chain walk currently linear-scans names; if a tight loop hits a deeply nested closure with a long chain, perf regresses (none observed). The compiler still allocates local slots for free-variable references, which means the function's own VariableStore also holds a (stale) shadow value for those names; eliminated when B.6.6 deletes VariableStore.
- 2026-05-23 — **B.6.5 done.** Top-level script frames now run on a `GlobalEnvironmentRecord` backed by the real realm global object, and `EnsureGlobalObject()` installs standard global properties (`globalThis`, constructors, numeric globals, URI functions, etc.) on that object with spec-shaped data descriptors. This closes the visible gap where `globalThis.x = 42; x;` returned `undefined` and where `Object === globalThis.Object` was false. Commit `898a2743`. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused global/env xUnit (`50/50`); full `FenBrowser.Js.Tests` (`1716/1716`); `FenBrowser.Js.Fuzz` (`35/35`); shell probes; exact test262 runtime files `built-ins/global/global-object.js` and `built-ins/global/property-descriptor.js` both passed.
- 2026-05-23 — **B.6.6 sub-slice: var declaration instantiation.** `BytecodeCompiler` records `var` declaration names on `BytecodeFunction`; `BytecodeInterpreter` creates those bindings in the active env record before executing bytecode; global `eval` now runs against the realm global env; the test262 runtime prelude exposes `$262.evalScript`. This closes `var brandNew; Object.hasOwn(globalThis, 'brandNew')` and exact test262 `language/global-code/script-decl-var.js`. Commit `55d168c4`. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused xUnit (`75/75`); full `FenBrowser.Js.Tests` (`1720/1720`); `FenBrowser.Js.Fuzz` (`35/35`); shell probes; exact test262 `script-decl-var.js` passed. Remaining B.6.6 work: migrate all remaining direct `VariableStore` reads/writes/captured-cell paths before deleting `VariableStore` and `JsVariableCell`.
- 2026-05-23 — **B.6.6 sub-slice: lexical declarations + TDZ.** Added lexical/const declaration metadata to `BytecodeFunction`, new `InitVar` bytecode for declaration initialization, TDZ-aware `LoadName`, const-assignment TypeError handling, and ReferenceError construction through the native-error surface. Top-level lexical declarations no longer mirror to `globalThis`. Commit `f1841652`. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused lexical/env/global/closure xUnit (`41/41`); full `FenBrowser.Js.Tests` (`1727/1727`); `FenBrowser.Js.Fuzz` (`35/35`); shell probes; exact test262 `language/eval-code/indirect/lex-env-no-init-let.js` and `lex-env-no-init-const.js` passed. Remaining B.6.6 work: remove `JsVariableCell` closure snapshotting and direct `frame.Variables` fallback after all declaration/function/global paths have env-record coverage.
- 2026-05-23 — **B.6.6 sub-slice: function declaration hoisting.** `BytecodeCompiler` now instantiates function declarations before bytecode statement execution and records them as var-style bindings so global/object-backed environment records own the visible binding. This closes source-order calls and sibling function resolution through the environment chain while leaving full `CreateGlobalFunctionBinding` property-redefinition semantics for a later focused slice. Commit `6f508f5c`. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused function/global/lexical/closure xUnit (`28/28`); full `FenBrowser.Js.Tests` (`1731/1731`); `FenBrowser.Js.Fuzz` (`35/35`); shell probes; exact test262 `language/global-code/decl-func.js` passed. Remaining B.6.6 work: remove `JsVariableCell` closure snapshotting and direct `frame.Variables` fallback after all declaration/function/global paths have env-record coverage.
- 2026-05-23 — **B.6.6 sub-slice: remove captured variable cells.** `JsFunctionObject` now retains only `OuterEnvironment`; `ExecuteInternal` no longer threads captured-variable dictionaries; `VariableStore` is a plain `JsValue[]`; `JsVariableCell` is deleted. `EnvironmentRecord.Trace(...)` now walks env bindings so captured heap objects survive GC through closure roots. Commit `2d5a197c`. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused closure/env/object xUnit (`160/160`); full `FenBrowser.Js.Tests` (`1732/1732`); `FenBrowser.Js.Fuzz` (`35/35`); exact test262 `language/expressions/arrow-function/arrow/capturing-closure-variables-1.js` passed. Remaining B.6.6 work: delete the direct `frame.Variables` fallback once all remaining slot-only reads/writes have environment coverage.
- 2026-05-23 — **B.6.6 sub-slice: builtin globals through env only.** Removed the interpreter's redundant `InitializeBuiltinGlobals` VariableStore preload; standard global names now resolve exclusively through the global object-backed `GlobalEnvironmentRecord`. Commit `782846a7`. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused global/closure/function xUnit (`22/22`); full `FenBrowser.Js.Tests` (`1733/1733`); `FenBrowser.Js.Fuzz` (`35/35`); exact test262 `built-ins/global/global-object.js` passed. Remaining B.6.6 work: delete parameter/arguments slot sync and the final direct `frame.Variables` fallback.
- 2026-05-23 — **D.6 done: Promise constructor + microtask checkpoint wired into interpreter.** Promise / Promise.resolve / Promise.reject / Promise.prototype.then / .catch / .finally now live on the global object as real native function objects; the `BytecodeInterpreter.Promises.cs` partial owns `PerformPromiseThen`, `CreateResolvingFunctions`, `FulfillPromise`/`RejectPromise`, `EnqueueReactionJob`, `RunPromiseReactionJob`, `RunPromiseResolveThenableJob`. `PromiseInstance` is the heap-resident `JsObject` carrying a `PromiseObject` slot. End-of-`Execute` microtask drain alternates between the queueMicrotask callback queue and the Promise `JobQueue` until both are empty so reactions enqueued inside the checkpoint complete in the same task. Self-resolution → TypeError rejection; thrown handler → child capability.reject; missing handler passes through (27.2.2.1 step 5/6). Commit `e717a110`. 14 new `PromiseRuntimeTests` (`PromiseRuntimeTests.cs`). 1750 main + 35 fuzz green. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused `PromiseRuntimeTests` (14/14); full `FenBrowser.Js.Tests` (1750/1750); `FenBrowser.Js.Fuzz` (35/35); pushed.

- 2026-05-23 — **Session honesty note.** User asked for "all next 5 actions" (D.6, H, E.6/E.7, F.5, C). Landed D.6 fully verified; declined to ship superficial H/E.6/F.5/C in the same session because Step H requires rewriting the class-body parser (currently skips body) + new opcodes + super runtime, and E.6 requires a module-aware parse + the spec Link/Evaluate algorithm — each comparable to D.6 in scope. Pausing for direction before continuing.

- 2026-05-23 — **F.5 done.** Host object dereferencing wired through `HostObjectTable` + `IHostHooks`. New partial `BytecodeInterpreter.Host.cs` owns `RequireHostObject`, `GetHostObjectProperty`, `SetHostObjectProperty`, `RegisterGlobalHostObject(name, handle)`. `GetReceiverProperty` grew a HostObject case; `SetPropByName` opcode forks on HostObject tag. Invalid handles (stale gen / cross-realm / navigated-away / dead slot) surface as JS TypeError; refused writes also surface as TypeError. Commit `fc0b8ef5`. 9 new `HostObjectIntegrationTests`. Verification: `dotnet build`; focused (9/9); full (1759/1759); fuzz (35/35); pushed.

- 2026-05-23 — **H.1 done.** Class declarations and class expressions now parse and execute. `ClassMemberNode` + `ClassMemberKind` AST node carries constructor / instance method / static method / getter / setter members. `JsParser.ParseClassBody` extracts members (replacing the prior body-skip loop); `BytecodeCompiler.CompileClassDeclaration` + `CompileClassExpressionToRegister` lower to existing opcodes (`CreateFunction` for the (synthesised default or explicit) constructor + `NewObject` for the prototype + per-method `CreateFunction` + `SetPropByName` install + `constructor`/`prototype` cross-link). `class Foo {}` binds via `InitVar`/lexical name. Commit `5c368f52`. 10 new `ClassRuntimeTests`. Verification: `dotnet build`; focused (10/10); full (1769/1769); fuzz (35/35); pushed. **Deferred:** H.2 extends/super (needs new opcode or runtime hook), H.3 getter/setter semantic dispatch, H.4 computed keys / private fields / static blocks.

- 2026-05-23 — **B.6.6 done: runtime frame storage removed.** Deleted `FenBrowser.Js/Interpreter/VariableStore.cs`; `InterpreterFrame` now carries only registers + `EnvironmentRecord`; parameter/arguments sync, Load/Store/Init, unresolvable reads/writes, and `delete identifier` run through env records/global object semantics. Commit `8bb30d8a`. Verification: `dotnet build FenBrowser.Js/FenBrowser.Js.csproj -c Debug -v minimal`; focused env/global/closure/function/object xUnit (`185/185`); full `FenBrowser.Js.Tests` (`1736/1736`); `FenBrowser.Js.Fuzz` (`35/35`); exact test262 `language/expressions/addition/S11.6.1_A2.1_T2.js` and `language/expressions/delete/11.4.1-3-1.js` passed. `rg` confirms no `VariableStore`, `JsVariableCell`, `frame.Variables`, or captured-variable map remains in FenJS code.
